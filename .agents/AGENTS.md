# Project Rules

## JS Editing
- After every edit to `static/script.js`, run a quick inline lint check for duplicate `const`/`let` declarations and missing HTML element IDs before finishing. Use a one-liner Python command — no separate linter file needed.
